/*
 * STACKIT Network Load Balancer API
 * This API offers an interface to provision and manage load balancing servers in your STACKIT project. It also has the possibility of pooling target servers for load balancing purposes.  For each load balancer provided, two VMs are deployed in your OpenStack project subject to a fee.
 *
 * The version of the OpenAPI document: 2.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package cloud.stackit.sdk.loadbalancer.model;

import cloud.stackit.sdk.loadbalancer.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;

/** SessionPersistence */
@javax.annotation.Generated(
		value = "org.openapitools.codegen.languages.JavaClientCodegen",
		comments = "Generator version: 7.19.0")
public class SessionPersistence {
	public static final String SERIALIZED_NAME_USE_SOURCE_IP_ADDRESS = "useSourceIpAddress";

	@SerializedName(SERIALIZED_NAME_USE_SOURCE_IP_ADDRESS)
	@javax.annotation.Nullable private Boolean useSourceIpAddress;

	public SessionPersistence() {}

	public SessionPersistence useSourceIpAddress(
			@javax.annotation.Nullable Boolean useSourceIpAddress) {
		this.useSourceIpAddress = useSourceIpAddress;
		return this;
	}

	/**
	 * If enabled then all connections from one source IP address are redirected to the same target.
	 * This setting changes the load balancing algorithm to Maglev.
	 *
	 * @return useSourceIpAddress
	 */
	@javax.annotation.Nullable public Boolean getUseSourceIpAddress() {
		return useSourceIpAddress;
	}

	public void setUseSourceIpAddress(@javax.annotation.Nullable Boolean useSourceIpAddress) {
		this.useSourceIpAddress = useSourceIpAddress;
	}

	@Override
	public boolean equals(Object o) {
		if (this == o) {
			return true;
		}
		if (o == null || getClass() != o.getClass()) {
			return false;
		}
		SessionPersistence sessionPersistence = (SessionPersistence) o;
		return Objects.equals(this.useSourceIpAddress, sessionPersistence.useSourceIpAddress);
	}

	@Override
	public int hashCode() {
		return Objects.hash(useSourceIpAddress);
	}

	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("class SessionPersistence {\n");
		sb.append("    useSourceIpAddress: ")
				.append(toIndentedString(useSourceIpAddress))
				.append("\n");
		sb.append("}");
		return sb.toString();
	}

	/**
	 * Convert the given object to string with each line indented by 4 spaces (except the first
	 * line).
	 */
	private String toIndentedString(Object o) {
		if (o == null) {
			return "null";
		}
		return o.toString().replace("\n", "\n    ");
	}

	public static HashSet<String> openapiFields;
	public static HashSet<String> openapiRequiredFields;

	static {
		// a set of all properties/fields (JSON key names)
		openapiFields = new HashSet<String>(Arrays.asList("useSourceIpAddress"));

		// a set of required properties/fields (JSON key names)
		openapiRequiredFields = new HashSet<String>(0);
	}

	/**
	 * Validates the JSON Element and throws an exception if issues found
	 *
	 * @param jsonElement JSON Element
	 * @throws IOException if the JSON Element is invalid with respect to SessionPersistence
	 */
	public static void validateJsonElement(JsonElement jsonElement) throws IOException {
		if (jsonElement == null) {
			if (!SessionPersistence.openapiRequiredFields
					.isEmpty()) { // has required fields but JSON element is null
				throw new IllegalArgumentException(
						String.format(
								java.util.Locale.ROOT,
								"The required field(s) %s in SessionPersistence is not found in the empty JSON string",
								SessionPersistence.openapiRequiredFields.toString()));
			}
		}

		Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
		// check to see if the JSON string contains additional fields
		for (Map.Entry<String, JsonElement> entry : entries) {
			if (!SessionPersistence.openapiFields.contains(entry.getKey())) {
				throw new IllegalArgumentException(
						String.format(
								java.util.Locale.ROOT,
								"The field `%s` in the JSON string is not defined in the `SessionPersistence` properties. JSON: %s",
								entry.getKey(),
								jsonElement.toString()));
			}
		}
		JsonObject jsonObj = jsonElement.getAsJsonObject();
	}

	public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
		@SuppressWarnings("unchecked")
		@Override
		public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
			if (!SessionPersistence.class.isAssignableFrom(type.getRawType())) {
				return null; // this class only serializes 'SessionPersistence' and its subtypes
			}
			final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
			final TypeAdapter<SessionPersistence> thisAdapter =
					gson.getDelegateAdapter(this, TypeToken.get(SessionPersistence.class));

			return (TypeAdapter<T>)
					new TypeAdapter<SessionPersistence>() {
						@Override
						public void write(JsonWriter out, SessionPersistence value)
								throws IOException {
							JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
							elementAdapter.write(out, obj);
						}

						@Override
						public SessionPersistence read(JsonReader in) throws IOException {
							JsonElement jsonElement = elementAdapter.read(in);
							validateJsonElement(jsonElement);
							return thisAdapter.fromJsonTree(jsonElement);
						}
					}.nullSafe();
		}
	}

	/**
	 * Create an instance of SessionPersistence given an JSON string
	 *
	 * @param jsonString JSON string
	 * @return An instance of SessionPersistence
	 * @throws IOException if the JSON string is invalid with respect to SessionPersistence
	 */
	public static SessionPersistence fromJson(String jsonString) throws IOException {
		return JSON.getGson().fromJson(jsonString, SessionPersistence.class);
	}

	/**
	 * Convert an instance of SessionPersistence to an JSON string
	 *
	 * @return JSON string
	 */
	public String toJson() {
		return JSON.getGson().toJson(this);
	}
}
