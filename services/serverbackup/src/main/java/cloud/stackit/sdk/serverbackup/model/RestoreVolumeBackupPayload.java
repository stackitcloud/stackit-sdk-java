/*
 * STACKIT Server Backup Management API
 * API endpoints for Server Backup Operations on STACKIT Servers.
 *
 * The version of the OpenAPI document: 2.0
 * Contact: support@stackit.de
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

package cloud.stackit.sdk.serverbackup.model;

import cloud.stackit.sdk.serverbackup.JSON;
import com.google.gson.Gson;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.annotations.SerializedName;
import com.google.gson.reflect.TypeToken;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.Arrays;
import java.util.HashSet;
import java.util.Map;
import java.util.Objects;
import java.util.Set;

/** RestoreVolumeBackupPayload */
@javax.annotation.Generated(
		value = "org.openapitools.codegen.languages.JavaClientCodegen",
		comments = "Generator version: 7.19.0")
public class RestoreVolumeBackupPayload {
	public static final String SERIALIZED_NAME_RESTORE_VOLUME_ID = "restoreVolumeId";

	@SerializedName(SERIALIZED_NAME_RESTORE_VOLUME_ID)
	@javax.annotation.Nonnull
	private String restoreVolumeId;

	public RestoreVolumeBackupPayload() {}

	public RestoreVolumeBackupPayload restoreVolumeId(
			@javax.annotation.Nonnull String restoreVolumeId) {
		this.restoreVolumeId = restoreVolumeId;
		return this;
	}

	/**
	 * Get restoreVolumeId
	 *
	 * @return restoreVolumeId
	 */
	@javax.annotation.Nonnull
	public String getRestoreVolumeId() {
		return restoreVolumeId;
	}

	public void setRestoreVolumeId(@javax.annotation.Nonnull String restoreVolumeId) {
		this.restoreVolumeId = restoreVolumeId;
	}

	@Override
	public boolean equals(Object o) {
		if (this == o) {
			return true;
		}
		if (o == null || getClass() != o.getClass()) {
			return false;
		}
		RestoreVolumeBackupPayload restoreVolumeBackupPayload = (RestoreVolumeBackupPayload) o;
		return Objects.equals(this.restoreVolumeId, restoreVolumeBackupPayload.restoreVolumeId);
	}

	@Override
	public int hashCode() {
		return Objects.hash(restoreVolumeId);
	}

	@Override
	public String toString() {
		StringBuilder sb = new StringBuilder();
		sb.append("class RestoreVolumeBackupPayload {\n");
		sb.append("    restoreVolumeId: ").append(toIndentedString(restoreVolumeId)).append("\n");
		sb.append("}");
		return sb.toString();
	}

	/**
	 * Convert the given object to string with each line indented by 4 spaces (except the first
	 * line).
	 */
	private String toIndentedString(Object o) {
		if (o == null) {
			return "null";
		}
		return o.toString().replace("\n", "\n    ");
	}

	public static HashSet<String> openapiFields;
	public static HashSet<String> openapiRequiredFields;

	static {
		// a set of all properties/fields (JSON key names)
		openapiFields = new HashSet<String>(Arrays.asList("restoreVolumeId"));

		// a set of required properties/fields (JSON key names)
		openapiRequiredFields = new HashSet<String>(Arrays.asList("restoreVolumeId"));
	}

	/**
	 * Validates the JSON Element and throws an exception if issues found
	 *
	 * @param jsonElement JSON Element
	 * @throws IOException if the JSON Element is invalid with respect to RestoreVolumeBackupPayload
	 */
	public static void validateJsonElement(JsonElement jsonElement) throws IOException {
		if (jsonElement == null) {
			if (!RestoreVolumeBackupPayload.openapiRequiredFields
					.isEmpty()) { // has required fields but JSON element is null
				throw new IllegalArgumentException(
						String.format(
								java.util.Locale.ROOT,
								"The required field(s) %s in RestoreVolumeBackupPayload is not found in the empty JSON string",
								RestoreVolumeBackupPayload.openapiRequiredFields.toString()));
			}
		}

		Set<Map.Entry<String, JsonElement>> entries = jsonElement.getAsJsonObject().entrySet();
		// check to see if the JSON string contains additional fields
		for (Map.Entry<String, JsonElement> entry : entries) {
			if (!RestoreVolumeBackupPayload.openapiFields.contains(entry.getKey())) {
				throw new IllegalArgumentException(
						String.format(
								java.util.Locale.ROOT,
								"The field `%s` in the JSON string is not defined in the `RestoreVolumeBackupPayload` properties. JSON: %s",
								entry.getKey(),
								jsonElement.toString()));
			}
		}

		// check to make sure all required properties/fields are present in the JSON string
		for (String requiredField : RestoreVolumeBackupPayload.openapiRequiredFields) {
			if (jsonElement.getAsJsonObject().get(requiredField) == null) {
				throw new IllegalArgumentException(
						String.format(
								java.util.Locale.ROOT,
								"The required field `%s` is not found in the JSON string: %s",
								requiredField,
								jsonElement.toString()));
			}
		}
		JsonObject jsonObj = jsonElement.getAsJsonObject();
		if (!jsonObj.get("restoreVolumeId").isJsonPrimitive()) {
			throw new IllegalArgumentException(
					String.format(
							java.util.Locale.ROOT,
							"Expected the field `restoreVolumeId` to be a primitive type in the JSON string but got `%s`",
							jsonObj.get("restoreVolumeId").toString()));
		}
	}

	public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
		@SuppressWarnings("unchecked")
		@Override
		public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
			if (!RestoreVolumeBackupPayload.class.isAssignableFrom(type.getRawType())) {
				return null; // this class only serializes 'RestoreVolumeBackupPayload' and its
				// subtypes
			}
			final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
			final TypeAdapter<RestoreVolumeBackupPayload> thisAdapter =
					gson.getDelegateAdapter(this, TypeToken.get(RestoreVolumeBackupPayload.class));

			return (TypeAdapter<T>)
					new TypeAdapter<RestoreVolumeBackupPayload>() {
						@Override
						public void write(JsonWriter out, RestoreVolumeBackupPayload value)
								throws IOException {
							JsonObject obj = thisAdapter.toJsonTree(value).getAsJsonObject();
							elementAdapter.write(out, obj);
						}

						@Override
						public RestoreVolumeBackupPayload read(JsonReader in) throws IOException {
							JsonElement jsonElement = elementAdapter.read(in);
							validateJsonElement(jsonElement);
							return thisAdapter.fromJsonTree(jsonElement);
						}
					}.nullSafe();
		}
	}

	/**
	 * Create an instance of RestoreVolumeBackupPayload given an JSON string
	 *
	 * @param jsonString JSON string
	 * @return An instance of RestoreVolumeBackupPayload
	 * @throws IOException if the JSON string is invalid with respect to RestoreVolumeBackupPayload
	 */
	public static RestoreVolumeBackupPayload fromJson(String jsonString) throws IOException {
		return JSON.getGson().fromJson(jsonString, RestoreVolumeBackupPayload.class);
	}

	/**
	 * Convert an instance of RestoreVolumeBackupPayload to an JSON string
	 *
	 * @return JSON string
	 */
	public String toJson() {
		return JSON.getGson().toJson(this);
	}
}
